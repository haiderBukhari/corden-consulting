/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['hr-backend-talo.com', 'media.licdn.com', 'www.cordenconsulting.com'],
    },
}

module.exports = nextConfig
